# Language setting
language: node_js

# Version number
node_js:
  - 0.12

build:
  ci:
    - ls -al

  on_failure:
    - echo "The job failed!!!!!"
    
  on_success:
    - echo "The job succeeded!!!!"
